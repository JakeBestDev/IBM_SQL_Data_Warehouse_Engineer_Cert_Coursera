-- Using DROP TABLE --
-- Please Note If Ran The PET Table will be DROPPED And Will Not Work Again, Will Need To Reload Data -- 

-- 1) Delete the PET table and verify if the table still exists or not (SELECT statement won’t work if a table doesn’t exist).
;
DROP TABLE PET;

SELECT * FROM PET;
;







